import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import VendingMachine from './VendingMachine/index';

ReactDOM.render(
  <React.StrictMode>
    <VendingMachine />
  </React.StrictMode>,
  document.getElementById('root')
);
